class Configuration:
    def __init__(self, verification_token, oauth_token, vhost):
        self.verification_token = verification_token
        self.oauth_token = oauth_token
        self.vhost = vhost
